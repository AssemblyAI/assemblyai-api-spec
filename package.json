{
  "license": "MIT",
  "scripts": {
    "lint": "fern check && spectral lint ./fern/{asyncapi,openapi}/*.yml --ruleset .spectral.yml",
    "format": "prettier ./fern/{asyncapi,openapi}/*.yml --write"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.11.0",
    "fern-api": "^0.14.3",
    "prettier": "^3.0.3"
  }
}
