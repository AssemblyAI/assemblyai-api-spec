{
  "license": "MIT",
  "scripts": {
    "lint": "spectral lint *.yml --ruleset .spectral.yml",
    "format": "prettier *.yml --write"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.11.0",
    "prettier": "^3.0.3"
  }
}
