---
title: Pre-Recorded Audio
description: Transcribe a pre-recorded audio file
---

import { Tabs, TabItem, Aside } from "@astrojs/starlight/components";

Our Speech-to-Text model enables you to transcribe pre-recorded audio into written text.

On top of the transcription, you can enable other features and models, such as [Speaker Diarization](/docs/pre-recorded-audio/speaker-diarization), by adding additional parameters to the same transcription request.

## Quickstart

The following example transcribes an audio file.

<Tabs>
<TabItem label="Python SDK">

```python
import assemblyai as aai

aai.settings.base_url = "https://api.assemblyai.com"
aai.settings.api_key = "YOUR_API_KEY"

# Use a publicly-accessible URL
audio_file = "https://assembly.ai/wildfires.mp3"

# Or use a local file:
# audio_file = "./example.mp3"

config = aai.TranscriptionConfig(
    speech_models=["universal-3-pro", "universal-2"],
    language_detection=True,
    speaker_labels=True,
)

transcript = aai.Transcriber().transcribe(audio_file, config=config)

if transcript.status == aai.TranscriptStatus.error:
    raise RuntimeError(f"Transcription failed: {transcript.error}")

print(f"\nFull Transcript:\n\n{transcript.text}")
```

</TabItem>
<TabItem label="JavaScript SDK">

```javascript
import { AssemblyAI } from "assemblyai";

const client = new AssemblyAI({ apiKey: "YOUR_API_KEY" });

const audioUrl = "https://assembly.ai/wildfires.mp3";

const transcript = await client.transcripts.transcribe({
  audio_url: audioUrl,
  speech_model: "best",
  language_detection: true,
  speaker_labels: true,
});

if (transcript.status === "error") {
  throw new Error(`Transcription failed: ${transcript.error}`);
}

console.log(transcript.text);
```

</TabItem>
<TabItem label="cURL">

```bash
# Submit transcription
curl https://api.assemblyai.com/v2/transcript \
  --header "Authorization: YOUR_API_KEY" \
  --header "Content-Type: application/json" \
  --data '{
    "audio_url": "https://assembly.ai/wildfires.mp3",
    "speech_model": "best",
    "language_detection": true,
    "speaker_labels": true
  }'
```

</TabItem>
</Tabs>

<Aside type="tip">
  You can also upload local files. Pass a local file path instead of a URL and
  the SDK will handle the upload automatically.
</Aside>

## Features

| Feature | Description |
|---------|-------------|
| [Model selection](/docs/pre-recorded-audio/select-the-speech-model) | Choose the best model for your use case |
| [Speaker diarization](/docs/pre-recorded-audio/speaker-diarization) | Identify who said what |
| [Language detection](/docs/pre-recorded-audio/language-detection) | Automatically detect the spoken language |
| [Prompting](/docs/pre-recorded-audio/prompting) | Guide transcription with natural language prompts |
| [Keyterms prompting](/docs/pre-recorded-audio/keyterms-prompting) | Boost accuracy for specific terms |

## Next steps

- [Select the right speech model](/docs/pre-recorded-audio/select-the-speech-model) for your use case
- Explore [Speech Understanding](/docs/speech-understanding/speaker-identification) for audio intelligence features
- Try the [LLM Gateway](/docs/llm-gateway/overview) to apply LLMs to your audio data
